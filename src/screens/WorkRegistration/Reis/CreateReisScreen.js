import { StyleSheet, Text, View } from "react-native";
import React from "react";

const CreateReisScreen = () => {
	return (
		<View>
			<Text>CreateReisScreen</Text>
		</View>
	);
};

export default CreateReisScreen;

const styles = StyleSheet.create({});
