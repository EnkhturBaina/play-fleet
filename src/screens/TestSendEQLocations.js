import { StyleSheet, Text, View } from "react-native";
import React from "react";

const TestSendEQLocations = () => {
	return (
		<View>
			<Text>TestSendEQLocations</Text>
		</View>
	);
};

export default TestSendEQLocations;

const styles = StyleSheet.create({});
